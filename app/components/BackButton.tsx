'use client';
import React from 'react'

const BackButton = () => {
  return (
    <div>
        <button className="btn btn-primary" onClick={() => console.log('Click')}>Back to Home</button>
    </div>
  )
}

export default BackButton
